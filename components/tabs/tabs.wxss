.tabs_title {
    display: flex;
}
.tabs_title_item {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
    color: #666;
    font-size: 28rpx;
    padding: 20rpx 0;
}
.active {
    color:var(--themeColor);
    border-bottom: 4rpx solid currentColor;
}
.tabs_content {}